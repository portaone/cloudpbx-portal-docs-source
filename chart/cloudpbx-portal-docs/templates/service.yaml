apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudpbx-portal-docs.fullname" . }}
  labels:
    {{- include "cloudpbx-portal-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudpbx-portal-docs.selectorLabels" . | nindent 4 }}
